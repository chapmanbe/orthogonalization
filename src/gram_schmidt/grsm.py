improt numpy as np

def unit_vector(v):
    """
    Takes a vector v and returns a unit vector form.

    v: numpy array

    Returns: numpy array

    Exceptions: ValueError
    """


def subtract_projection(a, q):
    """
    Takes two vectors and subtracts the projection of a onto q frrom a.

    a: numpy Array
    q: numpy Array

    returns: numpy Array

    Exceptions: ValueError if a and q are not the same dimension
    """


def gram_schmidt(a):
    """
    Takes a matrix and returns an orthonormal version using Gram-Shmidt \
    orthogonalization.

    a: mxn numpy Array

    Returns: a numpy Array

    Exceptipns: ValueError
    """


pass
